{% extends 'base.html.twig' %}

{% block title %}科目編集{% endblock %}

{% block body %}
{{ form_start(editForm, { action: path('subject_update', {id: subject.id}), method: 'POST' }) }}
  {{ form_row(editForm.name) }}
  {{ form_row(editForm.status) }}

  {{ form_row(editForm.save) }}
{{ form_end(editForm) }}

{{ form_start(deleteForm, { action: path('subject_update', {id: subject.id}), method: 'POST' }) }}
  <input type="hidden" name="_token" value="{{ csrf_token('subject_delete_' ~ subject.id) }}">
  {{ form_row(deleteForm.delete) }}
{{ form_end(deleteForm) }}

{% endblock %}